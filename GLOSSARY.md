---
title: "Глоссарий наименований экосистемы SDD"
date: 2026-08-18
status: accepted
tags: [glossary, naming, sdd]
---

# Наименования: SDD / Studio / Desktop / MPI

Канон имён экосистемы. Стек (Leptos, egui, Axum, Tauri) — в tech-доке, не в бренде.

> Переехал сюда 2026-08-18 из `sdd_studio/memory-bank/glossary/GLO__naming.md`.
> Studio — потребитель канона, а не его владелец.

## Три этажа

| Этаж | На экране | Аббр. | Расшифровка | Не говорить |
|---|---|---|---|---|
| Метод | Slice-Driven Design | **SDD** | Индексированный срез ведёт дизайн: тройка крейтов, роли, исполняемый каталог | VSA (как имя метода), ISA, Grid, Context-Driven |
| Инструмент | **SDD Studio** | — | Место работы над SDD-приложением | Designer, Builder, Dashboard, egui_designer_2, Code Grid |
| Инструмент | **SDD Desktop** | — | Десктоп-оболочка к SDD-приложению | mpi-desktop, клиент MPI |
| Продукт | **Integrator** | **MPI** | Marketplace Integrator — учёт и стыковка 1С с WB/Ozon/YM | Leptos Marketplace, leptos_marketplace_1 |

В речи: «MPI — SDD-приложение, открываю в Studio». «Studio» достаточно, если из
контекста ясно; в заголовке окна и README — **SDD Studio**.

DDD не переименовывается. DDD отвечает, *что* моделируем; SDD — *как это лежит*.

## Имена каталогов экосистемы

`F:\dev\<имя>`, все — члены одной экосистемы:

| Каталог | Что |
|---|---|
| `sdd` | канон (этот репозиторий) |
| `sdd_mpi_app` | приложение Integrator (MPI) |
| `sdd_studio` | анализатор |
| `sdd_desktop` | тонкий клиент |

Приставка `sdd_` = «член экосистемы». Суффикс `_app` отделяет приложение,
построенное по методу, от инструментов, обслуживающих метод. Будущее второе
SDD-приложение получает имя `sdd_<код>_app`.

Рабочие данные — не в репозитории, каталог называется **так же, как репозиторий**:
`F:\dev\sdd_mpi_app` ↔ `F:\data\sdd_mpi_app`. Одно имя в двух местах дешевле пары
«репо `sdd_mpi_app`, данные `mpi`», где приходится помнить соответствие.

## Поверхности имён

### SDD (метод)

| Поверхность | Канон | Сейчас |
|---|---|---|
| В речи и доке | SDD, Slice-Driven Design | местами VSA, «DDD+VSA» как синоним стандарта |
| Приложение по стандарту | SDD-приложение, SDD-проект | «leptos-проект», «VSA-проект» |
| Манифест правил | `architecture.toml` — **манифест SDD** | «конвенции», «проза architecture/» |
| Каталог объектов | `ARCHITECTURE.md` — **каталог SDD** | «карта проекта» |
| Глоссарий домена приложения | `CONTEXT.md` (это bounded context приложения, не имя метода) | — |
| Проверка правил | валидатор SDD (`check_architecture.ps1`) | «скрипт архитектуры» |
| Кэш/модель проекта | **`.sdd/`** (цель) | `.vsa_designer/` |
| Override студии | `.sdd/config.json` (цель) | `.vsa_designer/config.json` |

Манифест — источник правды в репозитории приложения. Override студии — локальные
пороги и исключения, **не второй стандарт**. Не называть оба «конфигом архитектуры».

### SDD Studio

| Поверхность | Канон | Статус |
|---|---|---|
| Окно, README, продукт | SDD Studio | сделано |
| Крейт / exe | `sdd_studio` | сделано |
| Тип приложения | `StudioApp` | сделано |
| Настройки / persist | `sdd_studio.settings.json` / `sdd_studio.state.json` | сделано |
| Папка репо | `F:\dev\sdd_studio` | сделано |
| CLI | `sdd_studio --analyze <Cargo.toml>` | сделано |
| Пункт меню артефактов | Open `.sdd` folder | цель; пока `.vsa_designer` |

Внутри Studio слово **Designer** можно оставить за будущим режимом «проектируем
объект». Это не имя продукта.

Технические имена пайплайна (`stage1_analyze`, `stage2_tree_ui`,
`stage3_diagnostics`) переименовывать не обязательно; пользователю они показываются
как **Analyze**, **Tree**, **Diagnostics**.

### SDD Desktop

| Поверхность | Канон | Статус |
|---|---|---|
| Продукт, README | SDD Desktop | сделано |
| Папка репо, exe | `sdd_desktop` | сделано |
| Bundle identifier | `com.sdd.desktop` | сделано (смена с `com.mpi.desktop` обнулила сохранённые профили) |

### Integrator / MPI

| Поверхность | Канон | Статус |
|---|---|---|
| Заставка, шапка | Integrator | сделано |
| `<title>`, папка деплоя | MPI | сделано |
| Расшифровка в конфиге | Marketplace Integrator | сделано |
| README, CLAUDE.md, гиды | Integrator (MPI) | сделано |
| Папка репо | `sdd_mpi_app` | сделано |
| GitHub | `teknatem/sdd_mpi_app` | сделано |
| npm-пакет ассетов | `mpi-ui` | сделано |
| Данные | `F:\data\sdd_mpi_app` | сделано |
| Служба Windows | MPI (или Integrator) | отложено |

Крейты `frontend` / `backend` / `contracts` **не переименовывать**: это origins, а
не бренд.

## Что уже названо правильно

Словарь SDD, не бренда. Живёт в `SDD.md` и в манифесте приложения.

| Имя | Смысл |
|---|---|
| Агрегат `aNNN` | доменный объект/документ |
| Use-case `uNNN` | именованная операция |
| Проекция `p9NN` | производная read-модель |
| Dashboard `d4NN`, схема `dsNN`, DataView `dvNNN`, задача `taskNNN` | остальные виды объектов |
| Origins B / C / F | backend / contracts / frontend |
| Слой-статус `slice` / `object` / `shared` | зеркало по крейтам / подсистема / сквозное |
| Роль | обязательный или опциональный файл в срезе (`repository.rs`, `metadata.json`, …) |
| `common` | разделяемое ядро внутри слоя, не сущность |
| Scene, LOD0/1/2, edges `related` / `use` | модель сцены Studio |
| `scene_model.json`, `diagnostics.json`, `project_files_index.csv` | имена артефактов Studio |

## Что сознательно не брендируем

- DDD, CQRS, MVVM — чужие школы, свои имена.
- Leptos, egui, Axum, Thaw, Tauri — стек.
- `CONTEXT.md` — глоссарий домена приложения; не путать с Context-Driven Design.

## Роли и артефакты работы

| Роль | Имя |
|---|---|
| Кто сидит в Studio | разработчик (не «дизайнер», если речь не про UI) |
| Единица работы | объект SDD (срез с кодом) |
| Прогон анализа | analyze, не «билд» |

## Порядок приведения в соответствие

1. ~~**Видимые имена Studio** — окно, README, крейт, класс, settings/persist.~~ (2026-08-18)
2. ~~**Дом канона** — вынести глоссарий из `sdd_studio/memory-bank/` в `F:\dev\sdd`.~~ (2026-08-18)
3. ~~**Каталог приложения** — `leptos_marketplace_1` → `sdd_mpi_app`, включая состояние
   инструментов, привязанное к пути.~~ (2026-08-18)
4. ~~**Видимые имена приложения** — README, CLAUDE.md, `package.json`, VSA как имя
   метода в memory-bank.~~ (2026-08-18)
5. **Remotes** — ~~GitHub-репо приложения~~ (2026-08-18); осталось: ни у Studio, ни у
   Desktop remote нет вовсе. У Desktop origin раньше указывал на мёртвый
   `E:\dev\rust\2\mpi-desktop` — его сняли, замену не завели.
6. ~~**Данные** — `F:\data\leptos_marketplace_1` → `F:\data\sdd_mpi_app`.~~ (2026-08-18)
   Оказалось дёшево: внутри данных абсолютных путей нет, переименование каталога +
   `[data].root` в `config.toml`. Целью значился `F:\data\mpi` — заменено на имя
   репозитория, чтобы не держать в голове два разных имени одного продукта.
7. **`.vsa_designer` → `.sdd`** — последним и **одним проходом по двум репозиториям**:
   ломает кэш, скрипты и стык №2. Вместе с ним — ключ метрики findings.

   Сторона Studio подготовлена (2026-08-18): имя было размазано литералом по
   14 файлам, теперь живёт одной константой `core::model::ARTIFACT_DIR` в
   `sdd_studio/src/core/model.rs`. Критерий, что подготовка держится:
   `grep -rn "vsa" --include=*.rs src/` в Studio даёт ровно одну строку — само
   определение константы. Проход теперь стоит по одной правке с каждой стороны.
